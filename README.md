# Prevent-User-Deletion-If-Assigned-To-An-Incident
Prevents deletion of user records in ServiceNow if they are assigned to active incidents using a Before Delete Business Rule and Glide Record validation to maintain data integrity and workflow consistency.
